<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/teal2</item>
        <item name="colorPrimaryDark">@color/teal2</item>
        <item name="colorAccent">@color/teal2</item>
        <item name="android:windowFullscreen">true</item>
        <item name="windowActionBar">true</item>
        <item name="android:colorPrimary">@color/teal2</item>
        <item name="android:colorPrimaryDark">@color/blue2</item>
        <item name="android:colorAccent">@color/teal1</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

</resources>
